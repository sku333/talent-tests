import { index, integer, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const users = sqliteTable('users',{anonymousId:text('anonymous_id').primaryKey(),source:text('source').notNull().default('direct'),firstSeenAt:integer('first_seen_at').notNull(),lastSeenAt:integer('last_seen_at').notNull(),completed:integer('completed',{mode:'boolean'}).notNull().default(false)});
export const events = sqliteTable('events',{id:integer('id').primaryKey({autoIncrement:true}),anonymousId:text('anonymous_id').notNull(),eventName:text('event_name').notNull(),source:text('source').notNull().default('direct'),testVersion:text('test_version').notNull().default('v1'),createdAt:integer('created_at').notNull()},t=>[index('idx_events_name_created').on(t.eventName,t.createdAt),index('idx_events_user').on(t.anonymousId)]);
export const results = sqliteTable('results',{id:integer('id').primaryKey({autoIncrement:true}),anonymousId:text('anonymous_id').notNull(),testVersion:text('test_version').notNull().default('v1'),typeName:text('type_name').notNull(),topOne:text('top_one').notNull(),topTwo:text('top_two').notNull(),topThree:text('top_three').notNull(),scoresJson:text('scores_json').notNull(),durationSeconds:integer('duration_seconds').notNull(),completedAt:integer('completed_at').notNull()},t=>[uniqueIndex('idx_results_user_version').on(t.anonymousId,t.testVersion),index('idx_results_completed').on(t.completedAt)]);
